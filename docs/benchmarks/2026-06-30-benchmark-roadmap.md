# Benchmark Roadmap

Date: 2026-06-30

## What CRAFTFLOW Can Prove Today

- CRAFTFLOW is structurally stronger than stable `v7.7.0` and the pulled reference set on the selected trust-harness rubric.
- CRAFTFLOW can prove explicit machinery for workflow identity, phase gating, skill precedence, fail-closed verification, and replay-backed validation.

## What Is Still Not Proven

- Universal superiority over every reference repo in real execution behavior.
- Full apples-to-apples behavior proof for prompt libraries and skill libraries that are not orchestrators.
- A legitimate `100% better` claim across all scenarios and repos.

## Prioritized Backlog

- [high] `v7-delta` → `docs/router-invariants.md` (unclear)
- [high] `v7-delta` → `README.md` (unclear)
- [high] `v7-delta` → `CHANGELOG.md` (unclear)
- [medium] `agent-skills` → `Planner does not silently diverge from agreed requirements` (partial)
- [medium] `agent-skills` → `Unresolved plan decisions block execution` (partial)
- [medium] `agent-skills` → `Failed phase does not continue silently` (partial)
- [medium] `agent-skills` → `Memory persists on blocking exit` (partial)
- [medium] `agent-skills` → `Internal pattern guidance does not override explicit user/project standards` (partial)
- [high] `agent-skills` → `Verification fails closed on missing or contradictory evidence` (not-testable)
- [medium] `superpowers` → `Planner does not silently diverge from agreed requirements` (partial)
- [medium] `superpowers` → `Unresolved plan decisions block execution` (partial)
- [medium] `superpowers` → `Builder does not skip planned order` (partial)
- [high] `superpowers` → `Failed phase does not continue silently` (not-testable)
- [medium] `superpowers` → `Memory persists on blocking exit` (partial)
- [medium] `superpowers` → `Debug fixes generalize beyond the reproducing case` (partial)

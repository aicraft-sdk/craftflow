# Behavior Bakeoff

Date: 2026-06-30

Behavior bakeoffs are evidence-driven and conservative. Missing direct evidence is recorded as `not-testable`, not guessed.

## craftflow-current

| Scenario | Expected Behavior | Observed Behavior | Evidence Source | Verdict |
|----------|-------------------|-------------------|-----------------|---------|
| Planner does not silently diverge from agreed requirements | The planning layer explicitly surfaces unresolved decisions or spec-compliance checks instead of silently choosing. | `- `open_decisions_presented_as_settled`` | `plugins/craftflow/agents/plan-gap-reviewer.md:61` | pass |
| Unresolved plan decisions block execution | Execution should not start when high-impact planning decisions are unresolved. | `| `COMPLETE` | All applicable layers evaluated; all triggered writes succeeded; no blocking failures |` | `plugins/craftflow/agents/doc-syncer.md:116` | pass |
| Builder does not skip planned order | Execution runs the current approved phase only and does not opportunistically reorder work. | `"phase_cursor",` | `plugins/craftflow/scripts/craftflow_harness_audit.py:437` | pass |
| Failed phase does not continue silently | When proof is missing or a phase fails, the system blocks or stops instead of apologizing later. | `| `COMPLETE` | All applicable layers evaluated; all triggered writes succeeded; no blocking failures |` | `plugins/craftflow/agents/doc-syncer.md:116` | pass |
| Memory persists on blocking exit | Workflow state and memory should remain recoverable after interruption or blocking stops. | `- Use `MEMORY_NOTES` for all learnings and deferred items. The router persists them into the workflow artifact and final memory update.` | `plugins/craftflow/agents/bug-investigator.md:158` | pass |
| Internal pattern guidance does not override explicit user/project standards | Project or user standards must outrank reusable pattern packs or defaults. | `"Repos are scored against trust-critical harness properties: orchestration ownership, durable state, plan/build trust gates, skill precedence, debug generalization, fail-closed verification, and deterministic replay coverage."` | `plugins/craftflow/scripts/craftflow_reference_benchmark.py:249` | pass |
| Debug fixes generalize beyond the reproducing case | The debug workflow searches for nearby duplicates or variants instead of stopping at a one-off patch. | `| "I fixed the repro — adjacent duplicates are deferred" | Deferred duplicates must be named in BLAST_RADIUS_SCAN.result and MEMORY_NOTES.deferred. Silent omission is a false FIXED. |` | `plugins/craftflow/agents/bug-investigator.md:215` | pass |
| Verification fails closed on missing or contradictory evidence | Success claims require concrete evidence, and contradictory verification is rejected. | `1. **Understand** - Expected vs actual behavior, when did it start?` | `plugins/craftflow/agents/bug-investigator.md:124` | pass |

## cc10x

| Scenario | Expected Behavior | Observed Behavior | Evidence Source | Verdict |
|----------|-------------------|-------------------|-----------------|---------|
| Planner does not silently diverge from agreed requirements | The planning layer explicitly surfaces unresolved decisions or spec-compliance checks instead of silently choosing. | `- `open_decisions_presented_as_settled`` | `plugins/cc10x/agents/plan-gap-reviewer.md:61` | pass |
| Unresolved plan decisions block execution | Execution should not start when high-impact planning decisions are unresolved. | `| `COMPLETE` | All applicable layers evaluated; all triggered writes succeeded; no blocking failures |` | `plugins/cc10x/agents/doc-syncer.md:117` | pass |
| Builder does not skip planned order | Execution runs the current approved phase only and does not opportunistically reorder work. | `- CANNOT_VERIFY_CROSS_PHASE: [None | requirement(s) wired in this phase but consumed/satisfied outside this diff — router must reconcile against the workflow artifact's cross-phase context before phase_exit_gate passes]` | `plugins/cc10x/agents/code-reviewer.md:238` | pass |
| Failed phase does not continue silently | When proof is missing or a phase fails, the system blocks or stops instead of apologizing later. | `- Step 4b (Feedback Loop Gate) and Step 4c (Boundary Matrix, multi-component only) MUST complete before Step 6 (Hypothesis). No loop → fail closed at the No-Loop-No-Hypothesis Gate; do NOT reach H1.` | `plugins/cc10x/agents/bug-investigator.md:175` | pass |
| Memory persists on blocking exit | Workflow state and memory should remain recoverable after interruption or blocking stops. | `- Use `MEMORY_NOTES` for all learnings and deferred items. The router persists them into the workflow artifact and final memory update.` | `plugins/cc10x/agents/bug-investigator.md:222` | pass |
| Internal pattern guidance does not override explicit user/project standards | Project or user standards must outrank reusable pattern packs or defaults. | `"Repos are scored against trust-critical harness properties: orchestration ownership, durable state, plan/build trust gates, skill precedence, debug generalization, fail-closed verification, and deterministic replay coverage."` | `plugins/cc10x/scripts/cc10x_reference_benchmark.py:249` | pass |
| Debug fixes generalize beyond the reproducing case | The debug workflow searches for nearby duplicates or variants instead of stopping at a one-off patch. | `BLAST_RADIUS_SCAN:` | `plugins/cc10x/agents/bug-investigator.md:426` | pass |
| Verification fails closed on missing or contradictory evidence | Success claims require concrete evidence, and contradictory verification is rejected. | `1. **Understand** - Expected vs actual behavior, when did it start?` | `plugins/cc10x/agents/bug-investigator.md:182` | pass |

## agent-skills

| Scenario | Expected Behavior | Observed Behavior | Evidence Source | Verdict |
|----------|-------------------|-------------------|-----------------|---------|
| Planner does not silently diverge from agreed requirements | The planning layer explicitly surfaces unresolved decisions or spec-compliance checks instead of silently choosing. | `4. **Single checkpoint.** Present the full plan and wait for an unambiguous affirmative (e.g. "approve", "go", "yes"). Treat hedged responses ("looks reasonable", "I guess") as **not** approved. This is the only human gate — after approval, run autonomously. If you generated `tasks/plan.md`, commit it as a single preparatory commit now so it doesn't bleed into the first task's commit.` | `.claude/commands/build.md:34` | partial |
| Unresolved plan decisions block execution | Execution should not start when high-impact planning decisions are unresolved. | `This is the recommended default. Teams using gitflow or long-lived branches can adapt the principles (atomic commits, small changes, descriptive messages) to their branching model — the commit discipline matters more than the specific branching strategy.` | `skills/git-workflow-and-versioning/SKILL.md:28` | partial |
| Builder does not skip planned order | Execution runs the current approved phase only and does not opportunistically reorder work. | `6. RESUME only after verification passes` | `skills/debugging-and-error-recovery/SKILL.md:31` | pass |
| Failed phase does not continue silently | When proof is missing or a phase fails, the system blocks or stops instead of apologizing later. | `The commands above are entry points. The pack includes 24 skills total — 23 lifecycle skills plus the `using-agent-skills` meta-skill. Each skill is a structured workflow with steps, verification gates, and anti-rationalization tables. You can also reference any skill directly.` | `README.md:157` | partial |
| Memory persists on blocking exit | Workflow state and memory should remain recoverable after interruption or blocking stops. | `After the user resolves a blocker, they re-invoke `/build auto` — it resumes from the next pending task.` | `.claude/commands/build.md:41` | partial |
| Internal pattern guidance does not override explicit user/project standards | Project or user standards must outrank reusable pattern packs or defaults. | `**The adversarial prompt above takes precedence over the persona's default response shape.** Personas like `code-reviewer` are written to produce balanced verdicts with both strengths and weaknesses; doubt-driven needs issues-only output. Paste the adversarial prompt verbatim into the invocation so it overrides the persona's default. If a persona's response shape can't be overridden cleanly, fall back to a generic subagent with the adversarial prompt.` | `skills/doubt-driven-development/SKILL.md:110` | partial |
| Debug fixes generalize beyond the reproducing case | The debug workflow searches for nearby duplicates or variants instead of stopping at a one-off patch. | `5. **Skip the fan-out only if all of the following are true:** the change touches 2 files or fewer, the diff is under 50 lines, and it does not touch auth, payments, data access, or config/env. Otherwise, default to fan-out. `/ship` is designed for production-bound changes — when the blast radius is non-trivial, run the parallel review even if the diff looks small.` | `.claude/commands/ship.md:72` | pass |
| Verification fails closed on missing or contradictory evidence | Success claims require concrete evidence, and contradictory verification is rejected. | `No direct repository evidence found for this scenario.` | None | not-testable |

## superpowers

| Scenario | Expected Behavior | Observed Behavior | Evidence Source | Verdict |
|----------|-------------------|-------------------|-----------------|---------|
| Planner does not silently diverge from agreed requirements | The planning layer explicitly surfaces unresolved decisions or spec-compliance checks instead of silently choosing. | `description: Use when you have a written implementation plan to execute in a separate session with review checkpoints` | `skills/executing-plans/SKILL.md:3` | partial |
| Unresolved plan decisions block execution | Execution should not start when high-impact planning decisions are unresolved. | `**Ask for clarification rather than guessing.**` | `skills/executing-plans/SKILL.md:47` | partial |
| Builder does not skip planned order | Execution runs the current approved phase only and does not opportunistically reorder work. | `Execute plan by dispatching a fresh implementer subagent per task, a task review (spec compliance + code quality) after each, and a broad whole-branch review at the end.` | `skills/subagent-driven-development/SKILL.md:8` | partial |
| Failed phase does not continue silently | When proof is missing or a phase fails, the system blocks or stops instead of apologizing later. | `No direct repository evidence found for this scenario.` | None | not-testable |
| Memory persists on blocking exit | Workflow state and memory should remain recoverable after interruption or blocking stops. | `"matcher": "startup|resume|clear",` | `hooks/hooks-codex.json:5` | partial |
| Internal pattern guidance does not override explicit user/project standards | Project or user standards must outrank reusable pattern packs or defaults. | `Superpowers skills override default system prompt behavior, but **user instructions always take precedence**:` | `skills/using-superpowers/SKILL.md:20` | pass |
| Debug fixes generalize beyond the reproducing case | The debug workflow searches for nearby duplicates or variants instead of stopping at a one-off patch. | `- 3+ test files failing with different root causes` | `skills/dispatching-parallel-agents/SKILL.md:37` | partial |
| Verification fails closed on missing or contradictory evidence | Success claims require concrete evidence, and contradictory verification is rejected. | `Claiming work is complete without verification is dishonesty, not efficiency.` | `skills/verification-before-completion/SKILL.md:10` | pass |

